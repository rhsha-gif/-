name = "aorch_scout"
description = "Fast read-only repository scout for bounded code mapping and evidence collection."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Perform only the assigned investigation. Do not spawn subagents or edit files. Return exact paths, symbols, commands, evidence, and uncertainty.
"""
