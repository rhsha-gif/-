interface:
  display_name: "Paper Lookup"
  short_description: "Discover scholarly sources with provenance"
  default_prompt: "Use $paper-lookup to discover reproducible scholarly sources for this chapter."

policy:
  allow_implicit_invocation: true
